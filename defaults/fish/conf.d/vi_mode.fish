# Optional Vi mode for Fish shell
# Only run in interactive sessions
status is-interactive; or exit

# Uncomment the following lines to enable Vi mode
# This provides Vi-style key bindings for command line editing

# Enable Vi mode
# fish_vi_key_bindings

# Set Vi as editor (optional - overrides nvim default)
# set -gx EDITOR vi
# set -gx VISUAL vi