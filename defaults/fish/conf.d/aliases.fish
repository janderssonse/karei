# Essential aliases - minimalist approach following host patterns
# Only the most practical aliases

# Tool replacements
alias vi='nvim'
alias vim='nvim'
alias ls='eza --icons'
alias bat='batcat'
alias docker='podman'
alias vimbare='nvim -u NONE -N'